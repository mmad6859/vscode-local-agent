---
description: Review the currently open/attached file for bugs and risks
---
Review the attached file for correctness bugs, edge cases it doesn't handle, and any risky patterns (e.g. unbounded loops, unhandled promise rejections, unsafe string handling). List concrete issues with line references — do not restate what the code does.
